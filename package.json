{
  "name": "json-xml-toolkit",
  "version": "1.0.0",
  "private": true,
  "description": "JSON/XML beautifier, minifier, validator and converter tools — static, dependency-free front end.",
  "scripts": {
    "start": "python3 -m http.server 8080",
    "build:bundle": "node tools/build-bundle.js",
    "test:smoke": "node smoke-test.js"
  },
  "devDependencies": {
    "jsdom": "^24.0.0"
  }
}
